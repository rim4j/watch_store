import 'package:flutter/material.dart';

class InitProfilePage extends StatelessWidget {
  const InitProfilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("init profile page"),
      ),
    );
  }
}
